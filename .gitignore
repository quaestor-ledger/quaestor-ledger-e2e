# Dependencies
node_modules/

# Test artifacts
test-results/
screenshots/
*.png
!fixtures/**/*.png
playwright-report/
.cache/

# Logs
*.log
npm-debug.log*

# Local environment
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
tmp
temp
tmp/worktrees
temp/worktrees

# BEGIN ores-sops dotenv policy — see env/README.md
# Plaintext dotenv is local-only at every depth, including suffix variants.
# Only env/enc/*.env.enc (sops ciphertext) is ever tracked; `just env-audit`
# enforces it.
.env
.env.*
*.env
**/*.env
**/**/*.env
*/*.env
*/**/*.env
*.env.*
!.env.example

# Decrypted material is never tracked.
env/dec
/env/dec/

# Only the approved ciphertext files are trackable.
/env/enc/*
!/env/enc/dev.env.enc
!/env/enc/prod.env.enc
# END ores-sops dotenv policy
env/dec/

# age private keys — never commit these.
*.agekey
*.age-key
keys.txt
sops-private*.txt

# Sanctioned scratch locations - AGENTS.md requires both, trailing-slash form
tmp/
temp/
